# Godot 4+
.godot/
/android/

# Export credentials (may contain secrets). Keep export_presets.cfg tracked.
export_credentials.cfg

# C# / Mono leftovers (this project uses GDScript)
.mono/
data_*/
mono_crash.*.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# Machine-specific VS Code settings (may contain a local Godot executable path)
.vscode/settings.json
